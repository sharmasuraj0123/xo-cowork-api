# Cache anchor

> This file exists to be a stable, identical-across-sessions string near the top of context. Prompt caches key on prefix bytes; a stable anchor improves cache hit rate.
>
> **Do not edit this file casually.** Every edit invalidates cache for every subsequent session. If you must edit, do it during a planned maintenance window, not mid-work.

---

cowork harness — context cache anchor v1
folder layout: AGENTS.md + OBJECTIVES.md + WORKSPACE.md + .xo/{state,memory,sessions,artifacts,skills,context}
loading discipline: stable prefix auto, heavy reads via subagent dispatch
memory model: semantic (distilled) + episodic (append-only) + procedural (validated twice) + working (session-scoped)
compression: cheap, count-based, predictable
