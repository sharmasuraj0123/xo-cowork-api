# XO Cowork API Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Data Validation
pydantic>=2.5.0

# Environment Variables
python-dotenv>=1.0.0

# HTTP Client (for external API calls)
httpx>=0.26.0
requests>=2.31.0

# Async support
anyio>=4.2.0

# Optional: For production deployment
gunicorn>=21.2.0
python-multipart>=0.0.20

# Composio — third-party tool integrations (Gmail, Stripe, Notion, ...)
composio>=0.7

# Model Context Protocol — local MCP server exposes composio meta-tools to
# OpenClaw / Hermes / Claude Code so gateways don't load every action upfront.
mcp>=1.2
