[pytest]
testpaths = tests
addopts = -q --strict-markers -p no:cacheprovider
asyncio_mode = auto
markers =
    live: needs the real claude CLI / a live server. Deselected by default.
filterwarnings =
    ignore::pytest.PytestUnraisableExceptionWarning
