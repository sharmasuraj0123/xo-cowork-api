---
description: GitHub workflow skills for managing repositories, pull requests, code reviews, issues, and CI/CD pipelines using the gh CLI and git via terminal. Authentication is pre-provisioned by xo-cowork-api and read from mcp-tokens.json (see github-auth) — no interactive login.
---
